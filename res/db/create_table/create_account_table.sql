CREATE TABLE account (id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,loginname VARCHAR(36) NOT NULL,mm VARCHAR(18) NOT NULL,xm VARCHAR(36) NOT NULL,roles VARCHAR(2) NOT NULL,lxfs VARCHAR(36);created_at TIMESTAMP,updated_at TIMESTAMP);
